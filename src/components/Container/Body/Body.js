import React from 'react';

const Body = (props) => 
     (
        <div className="arlo_tm_rightpart">
            <div className="rightpart_inner">
                {props.children}
            </div>
        </div>
    )


export default Body;
